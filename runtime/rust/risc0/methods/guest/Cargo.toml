[package]
name = "guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "1.3.0", default-features = false, features = [ "std" ] }
juvix = { path = "../../juvix" }
