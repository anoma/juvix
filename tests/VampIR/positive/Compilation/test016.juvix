-- higher-order functions
module test016;

open import Stdlib.Prelude;

f : (Nat → Nat) → Nat;
f g := g 5;

h : Nat → Nat → Nat;
h x z := x + z;

u : Nat → Nat → Nat;
u x y := f (h y) + x;

main : Nat → Nat → Nat;
main := u;
