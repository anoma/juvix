-- arithmetic
module test004;

import Stdlib.Prelude open;
import Stdlib.Data.Nat.Ord open;

f (x y : Nat) : Nat := x + y;

g (x y : Nat) : Nat := sub (3 * x) y;

h (f : Nat -> Nat -> Nat) (y z : Nat) : Nat := f y y * z;

func : Nat -> Nat := λ {x := x + 4};

vx : Nat := 30;

vy : Nat := 7;

main (x y z u : Nat) : Bool :=
  sub
      (func (div y x)
        + -- 17 div 5 + 4 = 7
            (z
            * x
          + y)
        + -- 17
          (vx
          + vy * (z + 1))
        + -- 37
          f
          (h g u 3)
          4)
      -- 16
      45
    == 32;
