-- letrec

def writeLn := \x write x >> write "\n";

-- def mutrec2 :=
--   let two := 2 in
--    letrec z2 := {
--        two
--     } in z2 + two;

def mutrec :=
  let two := 2 in
  letrec[f h]
    f := h;
    h := letrec z := {
       f + two
    } in z + two
  in writeLn f;

letrec x := 3 in mutrec
