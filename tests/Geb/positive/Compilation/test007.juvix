-- single-constructor inductive types
module test007;

open import Stdlib.Prelude;

type Box2 :=
    box2 : Nat -> Nat -> Box2;

type Box :=
    box : Box2 -> Box;

main : Nat;
main := case box (box2 3 5)
  | box (box2 x y) := x + y;
-- result: 8
