-- recursion through higher-order functions
module test017;

open import Stdlib.Prelude;

g : (Nat → Nat) → Nat → Nat;
g f zero := 0;
g f (suc x) := f x;

terminating
f : Nat → Nat;
f x := x + g f x;

main : Nat;
main := f 10; -- 55
