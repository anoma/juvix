module MutualLet;

import Stdlib.Data.Nat open;
import Stdlib.Data.Bool open;

main : _ :=
  let
    odd : _;
    even : _;
    odd zero := false;
    odd (suc n) := not (even n);
    even zero := true;
    even (suc n) := not (odd n);
  in even 5;
