module Symbols;

import Stdlib.Data.Nat open;

╘⑽╛ : Nat := suc 9;

-- no - function!?
- : Nat -> Nat -> Nat := (+);

（-） : Nat -> Nat -> Nat := (-);

（*） : Nat -> Nat -> Nat := (*);

syntax infixl 6 －;

－ : Nat -> Nat -> Nat := （-）;

syntax infixl 7 ·;

· : Nat -> Nat -> Nat := （*）;

（0） : Nat := ╘⑽╛ － ╘⑽╛ · zero;

主功能 : Nat := （0）;

axiom = : Type;

K : Nat → Nat → Nat
  | =a@zero = := =a · =
  | =a@(suc =) == := = · ==;
