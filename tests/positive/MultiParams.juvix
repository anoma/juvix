module MultiParams;

type Multi (A B C : Type) :=
  | mult : Multi A B C;

f :
  {A B : Type} → (C : Type) → {D E F : Type} → Type → Type;
f C _ := C;

g :
  {A B : Type} → (C : Type) → {D _ F : Type} → Type → Type;
g C _ := C;
