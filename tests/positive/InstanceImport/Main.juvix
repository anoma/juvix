module Main;

import M open;
import M open;

type Bool :=
  | true
  | false;

open Bool using {true; false} public;

instance
boolI : T Bool := mkT λ{x := x};

main : Bool := case T.pp unit of unit := T.pp true;
