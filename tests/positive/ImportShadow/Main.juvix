module Main;

import Nat open;

type Unit := unit : Unit;

f : Nat :=
  case unit
    | is-zero := zero;

f2 : Nat :=
  case suc zero
    | suc is-zero := zero
    | _ := zero;

f3 : Nat → Nat
  | (suc is-zero) := is-zero
  | zero := zero;
