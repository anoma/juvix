module main;

import Other;

type AVLTree (A : Type) :=
  node (AVLTree A)
       (AVLTree A)
       (AVLTree A)
       (AVLTree A)
       (AVLTree A)
       (AVLTree A)
       (AVLTree A)
       (AVLTree A)
       (AVLTree A);

type Foo := mkFoo (AVLTree Foo);
