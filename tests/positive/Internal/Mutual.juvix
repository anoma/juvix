module Mutual;

type Bool :=
  | true : Bool
  | false : Bool;

type Nat :=
  | zero : Nat
  | suc : Nat → Nat;

not : _
  | false := true
  | true := false;

odd : _
  
  | zero := false
  | (suc n) := even n;

even : _
  
  | zero := true
  | (suc n) := odd n;
