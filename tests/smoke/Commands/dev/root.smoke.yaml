working-directory: ./../../../../tests

tests:
  - name: show
    command:
      - juvix
      - dev
      - root
    stdout:
      matches: |
        .*?/(juvix|main)/tests/
      # contains "main" for the CI
    exit-status: 0
   