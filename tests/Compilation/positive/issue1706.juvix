module issue1706;
  open import Stdlib.Prelude public;

  f : Nat → Nat → Nat;
  f zero x := x;
  f (suc n) := id;

  g : Nat → Nat → Nat;
  g (suc n) := id;
  g zero x := x;


  x : IO;
  x := printNatLn (f 3 5);
  -- >> printNatLn (g 1 5);

  main : IO;
  main := x;
end;
