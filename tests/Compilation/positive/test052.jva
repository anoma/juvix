
function intToNat(integer) : integer;
function literalIntToInt(integer) : integer;
function myflip(* → * → *, *, *) : *;
function myconst(*, *) : *;
function mycompose(* → *, * → *, *) : *;
function main() : integer;
function lambda_12(integer) : integer;
function lambda_13(integer) : integer;
function lambda_14(integer) : integer;

function intToNat(integer) : integer {
  push arg[0];
  ret;
}

function literalIntToInt(integer) : integer {
  push arg[0];
  ret;
}

function myflip(* → * → *, *, *) : * {
  push arg[1];
  push arg[2];
  push arg[0];
  tccall 2;
}

function myconst(*, *) : * {
  push arg[0];
  ret;
}

function mycompose(* → *, * → *, *) : * {
  push arg[2];
  push arg[1];
  ccall 1;
  push arg[0];
  tccall 1;
}

function main() : integer {
  push 9;
  push 3;
  call lambda_14;
  add;
  ret;
}

function lambda_12(integer) : integer {
  push 1;
  push arg[0];
  add;
  ret;
}

function lambda_13(integer) : integer {
  push 2;
  push arg[0];
  mul;
  ret;
}

function lambda_14(integer) : integer {
  push 2;
  push arg[0];
  mul;
  pusht;
  push 1;
  push tmp[0];
  add;
  ret;
}
