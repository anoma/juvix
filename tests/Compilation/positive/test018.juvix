-- higher-order functions & recursion
module test018;

open import Stdlib.Prelude;

f : (Nat → Nat) → Nat;
f g := g 5;

h : Nat → Nat → Nat;
h x z := x + z;

u : Nat → Nat;
u x := f (h 4) + x;

main : IO;
main := printNatLn (u 2);

end;
