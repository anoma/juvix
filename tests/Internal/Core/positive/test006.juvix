module test006;

import Stdlib.Prelude open;
import Stdlib.Data.Nat.Ord open;

terminating
loop : Nat := loop;

e : Nat := (if (3 > 0) 1 loop) + (if (2 < 1) loop (if (7 >= 8) loop 1));

main : IO := printNatLn e;

end;
