module test006;

open import Stdlib.Prelude;
open import Stdlib.Data.Nat.Ord;

terminating
loop : Nat;
loop := loop;

e : Nat;
e := (if (3 > 0) 1 loop) + (if (2 < 1) loop (if (7 >= 8) loop 1));

main : IO;
main := printNatLn e;

end;
