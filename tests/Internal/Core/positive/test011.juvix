module test011;

open import Stdlib.Prelude;
open import Stdlib.Data.Nat.Ord;

fib' : Nat -> Nat -> Nat -> Nat;
fib' zero x _ := x;
fib' (suc n) x y := fib' n y (x + y);

fib : Nat -> Nat;
fib n := fib' n zero (suc zero);

main : IO;
main := printNatLn (fib (suc (suc (suc (suc (suc (suc (suc (suc (suc zero))))))))));

end;
